/*
    Divisors
*/

// Ask player for a number
let question = parseInt(prompt("Give in a number? "));

//check if number is positive, 1 = positive, -1 = negative
console.log(Math.sign(question));

// If number is positive then give all divisors

